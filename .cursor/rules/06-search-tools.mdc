---
description: Prefer ast-grep for code-aware searches; fall back to rg/grep for plain text; includes common ast-grep patterns.
globs:
  - **/*.{ts,tsx,js,jsx}
  - **/*.py
  - **/*.go
  - **/*.rb
  - **/*.{java,kt}
  - **/*.cs
  - **/*.php
  - **/*.rs
  - **/*.{c,h,cpp,hpp,cc,cxx}
  - **/*.swift
  - **/*.{m,mm}
  - **/*.lua
  - **/*.{sh,bash,zsh}
  - **/*.sql
  - **/*.r
  - **/*.scala
  - **/*.pl
  - **/*.dart
alwaysApply: false
---

# Search Tools
- Prefer **ast-grep** at `/opt/homebrew/bin/ast-grep` for code-aware searches.
- Use **rg** (ripgrep) or **grep** for plain text/logs/configs/docs or unsupported file types.
- Tool priority: **ast-grep → rg → grep**; choose based on file type/context.

## ast-grep Patterns
```bash
ast-grep --pattern 'function $NAME($ARGS) { $$$ }'
ast-grep --pattern 'class $NAME { $$$ }'
ast-grep --pattern 'import { $ITEMS } from "$MODULE"'
ast-grep --pattern 'const $VAR = $VALUE'
ast-grep --pattern '$OBJ.$METHOD($ARGS)'
```

## Usage
- This rule auto-attaches for the file types above; you can also pull it in manually via **@Cursor Rules** when planning refactors, audits, or symbol lookups.
